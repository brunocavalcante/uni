<%= form_for([:admin, @curso]) do |f| %>
  <% if @curso.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@curso.errors.count, "error") %> prohibited this curso from being saved:</h2>

      <ul>
      <% @curso.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <p>
    <%= f.label :nome %><br />
    <%= f.text_field :nome %>
  </p>
  <p>
    <%= f.label :categoria_curso_id %><br />
    <%= f.select :categoria_curso_id, CategoriaCurso.all.collect {|c| [ c.nome, c.id ] } %>
  </p>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
