<h1><%= @curso.nome %></h1>
<%= render :partial => 'admin/cursos/menu', :locals => { :curso => @curso } %>
<article>
  <header>
    <h2><%= t 'Disciplinas' %></h2>
    <ul id="toolbar">
      <li><%= link_to 'Nova Disciplina', new_admin_curso_disciplina_path(@curso), :class => 'new' %></li>
    </ul>
  </header>

  <table>
    <thead>
      <tr>
        <th>Nome</th>
      </tr>
    </thead>
    <tbody>
      <% @disciplinas.each do |disciplina| %>
      <tr class="<%= cycle("even", "odd") -%>">
        <td><%= link_to disciplina.nome, [:admin, @curso, disciplina] %></td>
      </tr>
      <% end %>
    </tbody>
  </table>
  
  <%= will_paginate @disciplinas %>
</article>
