<h1><%= t 'Alunos' %></h1>

<table>
  <tr>
    <th>Matrícula</th>
    <th><%= t 'Nome' %></th>
  </tr>

<% @alunos.each do |aluno| %>
  <tr class="<%= cycle("even", "odd") -%>">
    <td><%= aluno.matricula %></td>
    <td><%= link_to aluno.pessoa.nome, [:admin, aluno] %></td>
  </tr>
<% end %>
</table>

<%= will_paginate @alunos %>

<br />

<%= link_to 'Novo Aluno', new_admin_aluno_path %>
