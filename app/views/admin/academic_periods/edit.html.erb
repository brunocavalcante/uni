<h1><%= @academic_period.name %></h1>

<%= render :partial => 'menu', :locals => { :academic_period => @academic_period } %>
<article>
  <header>
    <h2><%= t 'Editing' %> <%= t 'activerecord.models.academic_period' %></h2>
    <ul id="toolbar">
      <li><%= link_to (t 'Show'), [:admin, @academic_period], :class => 'show' %></li>
      <li><%= link_to (t 'Back'), admin_academic_periods_path, :class => 'back' %></li>
    </ul>
  </header>

<%= render 'form' %>
</article>
