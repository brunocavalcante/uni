<header class="page-header">
  <h1><%= (t 'New') + ' ' + (t 'activerecord.models.academic_period') %></h1>
  <ul id="toolbar">
    <li><%= link_to (t 'Back'), admin_academic_periods_path, :class => 'back' %></li>
  </ul>
</header>

<%= render 'form' %>