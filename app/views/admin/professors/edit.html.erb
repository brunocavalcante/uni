<header>
  <h1><%= t 'Edit' %> <%= t 'Professor' %></h1>
  <ul id="toolbar">
    <li><%= link_to 'Show', [:admin, @professor], :class => 'show' %></li>
    <li><%= link_to (t 'Back'), admin_professors_path, :class => 'back' %></li>
  </ul>
</header>

<%= render 'form' %>