<header>
  <h1><%= t 'Students' %></h1>
  <ul id="toolbar">
    <li><%= link_to ((t 'New') + ' ' + (t 'Student')), new_admin_student_path, :class => 'new' %></li>
  </ul>
</header>

<table>
  <thead>
    <tr>
      <th><%= t 'Code' %></th>
      <th><%= t 'Name' %></th>
    </tr>
  </thead>

  <tbody>
    <% @students.each do |student| %>
    <tr>
      <td><%= student.code %></td>
      <td><%= link_to student.name, [:admin, student] %></td>
    </tr>
    <% end %>
  </tbody>
</table>

<%= will_paginate @students %>