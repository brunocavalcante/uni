%header.page-header
  %h1= (t 'New') + ' ' + (t 'activerecord.models.student')
  %ul#toolbar
    %li= link_to (t 'Back'), admin_students_path, :class => 'back'

%div{:class => 'block-message alert-message warning'}
  = t 'PasswordWillBeTheStudentCode'

= render 'form'