<header>
  <h1><%= t 'Editing' %> <%= t 'activerecord.models.student' %></h1>
  <ul id="toolbar">
    <li><%= link_to (t 'Back'), [:admin, @student], :class => 'back' %></li>
  </ul>
</header>

<%= render 'form' %>