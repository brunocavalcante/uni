<header>
  <h1><%= t 'Edit' %> <%= t 'Student' %></h1>
  <ul id="toolbar">
    <li><%= link_to (t 'Show'), [:admin, @student], :class => 'show' %></li>
    <li><%= link_to (t 'Back'), admin_students_path, :class => 'back' %></li>
  </ul>
</header>

<%= render 'form' %>