<h1><%= @curso.nome %></h1>
<%= render :partial => 'admin/cursos/menu', :locals => { :curso => @curso } %>
<article>
  <h2>Alunos</h2>
  
  <table>
    <thead>
      <tr>
        <th>Matrícula</th>
        <th>Nome</th>
      </tr>
    </thead>
  
    <tbody>
      <% @alunos.each do |aluno| %>
      <tr>
        <td><%= aluno.matricula %></td>
        <td><%= link_to aluno.nome, [:admin, aluno] %></td>
      </tr>
      <% end %>
    </tbody>
  </table>
  
  <%= will_paginate @alunos %>
</article>
