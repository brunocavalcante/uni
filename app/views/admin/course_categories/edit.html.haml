- content_for :title do
  %h1= t 'Parameters'
  
= render :partial => 'admin/parameters/menu'

%article.content
  %header.page-header
    %h2= (t 'Editing') + ' ' + (t 'activerecord.models.course_category')
    %ul#toolbar
      %li= link_to (t 'Back'), {:action => 'show'}, :class => 'back'
      
  = render 'form'