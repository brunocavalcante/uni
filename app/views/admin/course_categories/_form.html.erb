<%= form_for([:admin, @course_category]) do |f| %>
  <%= f.error_messages %>

  <p>
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </p>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
