<h1><%= @course.name %></h1>
<%= render :partial => 'admin/courses/menu', :locals => {:course => @course} %>
<article>
  <header>
    <h2><%= t 'AddDisciplineToCurriculum' %></h2>
    <ul id="toolbar">
      <li><%= link_to (t 'Back'), admin_course_curriculum_path(@course, @curriculum), :class => 'back' %></li>
  </header>
  
  <%= form_tag({:action => 'create'}) do %>
    <p>
      <%= label_tag :module, (t 'activerecord.attributes.curriculum_discipline.name') %><br />
      <%= text_field_tag :module %>
    </p>
  
    <div class="actions">
      <%= submit_tag (t 'AddSelected') %>
    </div>
    
    <table>
      <thead>
        <tr>
          <th></th>
          <th><%= t 'activerecord.attributes.discipline.code' %></th>
          <th><%= t 'activerecord.attributes.discipline.name' %></th>
          <th><%= t 'activerecord.attributes.discipline.credits' %></th>
          <th><%= t 'activerecord.attributes.discipline.version' %></th>
        </tr>
      </thead>
      <tbody>
        <% @disciplines.each do |discipline| %>
        <tr>
          <td><%= check_box_tag 'disciplines[]', discipline.id %></td>
          <td><%= discipline.code %></td>
          <td><%= link_to discipline.name, [:admin, @course, discipline] %></td>
          <td><%= discipline.credits %></td>
          <td><%= discipline.version %></td>
        </tr>
        <% end %>
      </tbody>
    </table>
    
    <%= will_paginate @disciplines %>
  <% end %>
</article>
