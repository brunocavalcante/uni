<h1><%= t 'Parameters' %></h1>
<%= render :partial => 'admin/parameters/menu' %>
<article>
  <header>
    <h2><%= t 'Editing' %> <%= t 'activerecord.models.scholarity' %></h2>
    <ul id="toolbar">
      <li><%= link_to (t 'Show'), {:action => 'show', :id => @scholarity.id}, :class => 'show' %></li>
      <li><%= link_to (t 'Back'), {:action => 'index'}, :class => 'back' %></li>
    </ul>
  </header>

  <%= render 'form' %>
</article>
