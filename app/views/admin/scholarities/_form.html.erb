<%= form_for([:admin, @scholarity]) do |f| %>
  <%= f.error_messages %>

  <p>
    <%= f.label :name, (t 'Name') %><br />
    <%= f.text_field :name %>
  </p>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
