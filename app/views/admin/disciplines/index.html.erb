<h1><%= @course.name %></h1>
<%= render :partial => 'admin/courses/menu', :locals => { :course => @course } %>
<article>
  <header>
    <h2><%= t 'Disciplines' %></h2>
    <ul id="toolbar">
      <li><%= link_to ((t 'New') + ' ' + (t 'Discipline')), new_admin_course_discipline_path(@course), :class => 'new' %></li>
    </ul>
  </header>

  <table>
    <thead>
      <tr>
        <th><%= t 'Code' %></th>
        <th><%= t 'Name' %></th>
        <th><%= t 'Credits' %></th>
      </tr>
    </thead>
    <tbody>
      <% @disciplines.each do |discipline| %>
      <tr>
        <td><%= discipline.code %></td>
        <td><%= link_to discipline.name, [:admin, @course, discipline] %></td>
        <td><%= discipline.credits %></td>
      </tr>
      <% end %>
    </tbody>
  </table>
  
  <%= will_paginate @disciplines %>
</article>
