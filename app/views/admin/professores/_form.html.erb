<%= form_for([:admin, @professor]) do |f| %>
  <% if @professor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@professor.errors.count, "error") %> prohibited this professor from being saved:</h2>

      <ul>
      <% @professor.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.fields_for :pessoa do |p| %>
  <div class="field">
    <%= p.label :nome %><br />
    <%= p.text_field :nome %>
  </div>
  <div class="field">
    <%= p.label :email %><br />
    <%= p.text_field :email %>
  </div>
  <% end %>
  <div class="field">
    <%= f.label :escolaridade_id %><br />
    <%= f.select :escolaridade_id, Escolaridade.all.collect {|it| [it.nome, it.id]} %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
