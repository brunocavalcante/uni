<%= form_for([:admin, @course]) do |f| %>
  <%= f.error_messages %>

  <p>
    <%= f.label :code %><br />
    <%= f.text_field :code %>
  </p>
  <p>
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </p>
  <p>
    <%= f.label :course_category_id %><br />
    <%= f.select :course_category_id, CourseCategory.all.collect {|c| [ c.name, c.id ] } %>
  </p>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
