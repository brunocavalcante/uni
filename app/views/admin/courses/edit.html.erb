<h1><%= @course.name %></h1>

<%= render :partial => 'menu', :locals => { :course => @course } %>
<article>
  <header>
    <h2><%= t 'Editing' %> <%= t 'activerecord.models.course' %></h2>
    <ul id="toolbar">
      <li><%= link_to (t 'Show'), [:admin, @course], :class => 'show' %></li>
      <li><%= link_to (t 'Back'), admin_courses_path, :class => 'back' %></li>
    </ul>
  </header>

  <%= render 'form' %>
</article>
