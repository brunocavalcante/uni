<% content_for :title do %><h1><%= @course.name %></h1><% end %>
<%= render :partial => 'admin/courses/menu', :locals => {:course => @course} %>
<article class="content">
  <header class="page-header">
    <h2><%= t 'New' %> <%= t 'activerecord.models.curriculum' %></h2>
    <ul id="toolbar">
      <li><%= link_to (t 'Back'), admin_course_curriculums_path(@course), :class => 'back' %></li>
    </ul>
  </header>

  <%= render 'form' %>
</article>