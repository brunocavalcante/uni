<%= form_for([:professor, @lecture, @test]) do |f| %>
  <%= f.error_messages %>

	<p>
		<%= f.label :name %><br/>
		<%= f.text_field :name %>
	</p>

	<p>
		<%= f.label :date %><br/>
		<%= f.datetime_select :date %>
	</p>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
