- content_for :title do
  %h1= @lecture.name
  
= render :partial => 'professor/lectures/menu'

%article.content
  %header.page-header
    %h2= (t 'New') + ' ' + (t 'activerecord.models.test')
    %ul#toolbar
      %li= link_to (t 'Back'), professor_lecture_tests_path(@lecture), :class => 'back'

  = render 'form'