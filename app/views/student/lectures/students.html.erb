<h1><%= @lecture.name %></h1>
<%= render :partial => 'menu' %>
<article>
  <h2><%= t 'Students' %></h2>
  
  <table>
    <thead>
      <tr>
        <th>Matrícula</th>
        <th>Nome</th>
      </tr>
    </thead>

    <tbody>
      <% for lecture_student in @lecture_students do %>
      <tr>
        <td><%= lecture_student.student.code %></td>
        <td><%= link_to lecture_student.name, user_path(lecture_student.student.person) %></td>
      </tr>
      <% end %>
    </tbody>
  </table>
  
  <%= will_paginate @lecture_students %>
</article>